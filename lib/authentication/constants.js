const DEFAULT_TOKEN_TTL = 30 * 60 * 1000;
const DEFAULT_AUTH_TIMEOUT = 60 * 5 * 1000;
const JOIN_CHAR = '&';
const ADAPTERS = {
  'basic': true
};

module.exports = {
  DEFAULT_TOKEN_TTL,
  DEFAULT_AUTH_TIMEOUT,
  JOIN_CHAR,
  ADAPTERS
};